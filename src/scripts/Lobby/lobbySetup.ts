import lobbyObject from "./lobby";
import "./lobby.scss";
import store from "../Store/store";

lobbyObject;

// store.subscribe(() => {
//   console.log(store.getState().gameState);
// });

export default "";
